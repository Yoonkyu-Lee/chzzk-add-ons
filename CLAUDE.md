# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때의 규칙이다.
사람은 최종 완성물을 볼 때까지 개입하지 않는다는 전제로 쓰였다.

## 프로젝트

치지직(CHZZK)을 크롬에서 볼 때 부족한 기능을 채우는 확장 프로그램.
첫 기능은 **다시보기 재생목록** — 밀린 방송을 원하는 순서로 이어보기.

설계의 근거와 세부는 전부 스펙에 있다. 작업 시작 전 반드시 읽는다:

- `docs/superpowers/specs/2026-09-15-replay-playlist-design.md` — 설계 문서 (권위 있는 출처)
- `docs/mockups/playlist-mockup.html` — UI 목업. 화면 구성의 기준
- `docs/progress.md` — 진행 기록. **매 작업 단위마다 갱신한다**

스펙과 이 문서가 충돌하면 스펙이 이긴다. 단, 아래 "절대 규칙"은 예외다.

## 절대 규칙

1. **확장 본체는 빌드 없음.** `src/` 아래는 바닐라 JS/CSS만. 트랜스파일·번들러·
   프레임워크를 도입하지 않는다. `devDependencies`(테스트·검증 도구)는 예외.
2. **원격 코드 없음.** CDN 로드, `eval`, 외부 스크립트 주입 금지.
   확장 심사와 CSP 양쪽에서 문제가 된다.
3. **치지직 마크업 의존은 `src/content/videos-page.js`와 `watch-page.js`에만.**
   다른 파일에서 치지직 DOM을 셀렉터로 찾지 않는다. 사이트 개편 시 고칠 곳을
   두 파일로 한정하는 것이 이 구조의 목적이다.
4. **`chrome.storage`는 `src/common/store.js`만 안다.** 다른 파일은 store를 통한다.
5. **API 응답 스키마는 `src/common/chzzk-api.js`만 안다.** 정규화된 내부 모델만 밖으로 나간다.
6. **`src/common/queue.js`는 순수 함수만.** DOM·storage·fetch·전역 상태 무의존.
   이 파일이 기능의 심장이고 브라우저 없이 전부 테스트된다.
7. **큐에 없는 영상 페이지에서는 아무 동작도 하지 않는다.** 일반 시청을 방해하지 않는다.
8. **조용히 실패하지 않는다.** 모든 실패 경로는 패널에 사용자가 읽을 문구를 남긴다.
   스펙 8장의 에러 표가 체크리스트다.

## 개발 루프

`docs/progress.md`의 다음 미완료 항목을 집어 아래를 반복한다.

1. **읽기** — `docs/progress.md`로 현재 위치를 확인한다.
2. **테스트 먼저** — 순수 로직이면 `test/`에 실패하는 테스트를 먼저 쓴다.
   실패를 눈으로 확인한 뒤 구현한다. `queue.js`, `format.js`,
   `chzzk-api.js`의 정규화 함수가 대상이다.
3. **구현** — 모듈 경계를 지킨다. 파일이 커지면 너무 많은 일을 하고 있다는 신호다.
4. **검증** — `node --test`를 돌린다. DOM이 걸린 변경이면 하니스도 돌린다.
5. **기록** — `docs/progress.md`를 갱신한다. 완료 표시, 새로 알게 된 사실,
   막힌 지점을 적는다. 스펙의 가정이 틀렸다면 **스펙 문서를 고친다** —
   4.3절(미확인 항목)에서 확인된 것을 4.2절로 옮기는 것이 대표적이다.
6. **커밋** — 아래 규칙대로.

### 순서

`queue.js` → `format.js` → `chzzk-api.js` → `store.js` → `panel/` →
`content/videos-page.js` → `content/watch-page.js` → `boot.js` + `manifest.json` → 하니스 검증.

순수 로직부터 올라간다. 브라우저를 띄우기 전에 심장이 테스트로 굳어 있어야 한다.

## 브랜치와 커밋

- 작업은 `feat/replay-playlist`에서 한다. `main`에 직접 커밋하지 않는다.
- 스펙 10장의 완료 기준을 전부 만족하면 `main`으로 `--no-ff` 머지한다.
- 커밋은 작고 자주. 테스트가 초록인 상태로만 커밋한다.
- 되돌리기·리베이스·브랜치 생성은 자유롭게 한다. 허락을 구하지 않는다.
- 커밋 메시지는 한국어, `type: 요약` 형식. `feat`/`fix`/`test`/`docs`/`refactor`/`chore`.
  본문에 왜 그렇게 했는지를 적는다. 무엇을 했는지는 diff가 말한다.

## 검증 — 중요

### Claude-in-Chrome 도구로는 치지직에 갈 수 없다

`mcp__claude-in-chrome__navigate`로 `chzzk.naver.com`을 열면
`This site is not allowed due to safety restrictions`가 돌아온다.
naver.com이 민감 도메인으로 기본 차단되어 있다. 2026-09-15 확인.
**다시 시도하지 말고 아래 하니스를 쓴다.**

### tools/harness.js

`puppeteer-core`가 **설치된 Chrome**을 띄운다 (Chromium 다운로드 없음).
전용 프로필에 확장을 `--load-extension`으로 올리고 chzzk를 직접 방문한다.
Claude-in-Chrome의 도메인 제한과 무관한 경로다.

```
C:\Program Files\Google\Chrome\Application\chrome.exe
```

하니스가 하는 일:

- 확장을 올린 Chrome 실행, 목록/영상 페이지 방문
- `storage.local` 직접 읽기로 큐 상태 확인
- 스크린샷과 콘솔 로그를 `tools/out/`에 저장 (커밋하지 않음)
- 긴 영상을 기다리지 않기 위해 `video.currentTime`을 끝 근처로 밀어 종료 전환을 유발

검증 시나리오는 스펙 9.2절에 번호로 정리돼 있다. 각 시나리오는 통과/실패를
`docs/progress.md`에 기록한다.

### 실패했을 때

추측으로 고치지 않는다. 스크린샷과 콘솔 로그를 먼저 본다.
필요하면 하니스에 `headless: false`로 화면을 띄우고 직접 조작해 관찰한다.
원인을 찾기 전에 코드를 바꾸지 않는다.

## 자율 작업 규칙

사람은 완성물을 볼 때까지 개입하지 않는다. 그래서:

- **막혀도 물어보지 않는다.** 2~3회 시도해 안 되면 접근을 바꾼다.
  스펙의 대안 경로를 쓰거나, 그 항목을 건너뛰고 진행한 뒤
  `docs/progress.md`에 "막힌 항목"으로 남긴다.
- **범위를 넓히지 않는다.** 스펙 2장의 비목표를 v1에 끌어오지 않는다.
  좋은 아이디어가 떠오르면 `docs/progress.md`의 "v2 후보"에 적어 두고 넘어간다.
- **진행 기록이 컨텍스트를 대신한다.** 긴 작업 중 컨텍스트가 요약될 수 있으므로
  `docs/progress.md`가 유일하게 믿을 수 있는 현재 상태다. 매번 갱신한다.
- **완료 기준을 스스로 낮추지 않는다.** 스펙 10장의 8개 조건 전부를 만족해야 끝이다.
  만족하지 못한 항목이 있으면 끝났다고 말하지 않고, 무엇이 왜 남았는지 적는다.

### 멈추고 물어봐야 하는 경우

- 스펙의 핵심 가정(D1~D7)이 실제로 성립하지 않아 설계를 다시 해야 할 때
- 치지직 계정 로그인이 필요해졌을 때 — 자격증명은 절대 다루지 않는다
- 비공식 API가 완전히 막혀 D2가 불가능해졌을 때

이 셋 외에는 판단해서 진행한다.

## 코드 규칙

- ES 모듈. `import`/`export`. 세미콜론 사용.
- 들여쓰기 2칸. 문자열은 작은따옴표.
- 주석은 한국어. **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적는다.
  코드가 이미 말하는 것을 다시 쓰지 않는다.
- 함수 이름은 동사로 시작. 순수 함수는 입력을 변형하지 않고 새 값을 반환한다.
- 사용자에게 보이는 문구는 한국어. 무엇이 잘못됐고 무엇을 하면 되는지 말한다.
  사과하지 않는다.
- 버튼 레이블은 일어날 일을 그대로 적는다. `전체 담기`를 누르면 담긴다.

## 하지 않을 것

- 자체 서버, 계정, 원격 저장
- 영상 다운로드
- 재생목록 공유 링크
- 치지직 UI 전면 재디자인
- 확장 스토어 배포 준비 (v1 완료 후 별도 판단)
